#include "main.h"
/**
 * _is lower -fuction to check if
 * character is lowercase
 *
 * @c: checks input of function
 * Return: return 1 if c is lowercase
 * otherwise always 0 (success)
*/

int _is lower(int c)
{
if (c >= 97 && c <= 122)
return (1);
return (0);
}
